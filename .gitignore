# Dependencies
node_modules/
.pnpm-store/
.yarn/
.npm/

# Build outputs
dist/
build/
out/
.next/
.cache/
.turbo/

# CDK
cdk.out/
cdk.context.json

# Expo / React Native
.expo/
.expo-shared/
*.orig.*

# Testing
coverage/
.nyc_output/
playwright-report/
test-results/

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Environment variables (NEVER commit)
.env
.env.local
.env.*.local
.env.production
.env.development

# IDE / Editor
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.swp
*.swo

# OS
.DS_Store
.DS_Store?
._*
Thumbs.db
desktop.ini

# TypeScript
*.tsbuildinfo

# Linter / formatter caches
.eslintcache
.prettiercache

# Husky internals (generated by husky install)
.husky/_/

# Prisma
prisma/.env

# Temporary
tmp/
temp/

# Database
*.sqlite
*.db

# Secrets
*.pem
*.key
!*.pub

# Firebase (FCM client config kept out of the public repo; EAS builds get it
# via the GOOGLE_SERVICES_JSON file env var — see packages/mobile/app.config.js)
google-services.json
*firebase-adminsdk*.json

# Local-only assets (per-developer, e.g. downloaded TLS CA cert
# for prod pooler verification — see packages/api/.env.example).
.local/

# Superpowers brainstorming companion (per-session mockups + state)
.superpowers/

# Claude Code per-developer settings (plugin enable/disable, local prefs)
.claude/settings.local.json
